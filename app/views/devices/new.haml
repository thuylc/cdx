- content_for(:subheader) do
  .devicearea
    .row.center.collapse.add-new
      .large-12.columns
        .col
          %h2
            = link_to devices_path, class: 'side-link', title: 'Back' do
              = image_tag "arrow-left-white.png"
            = I18n.t('devices.new.new_device')

.formContainer
  = render 'form'
